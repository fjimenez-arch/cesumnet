// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build
export default defineConfig({
  // Cuando publiques, cambia esto por tu dominio real:
  site: 'https://cesumnet.com',
});
